<%= render "application/main",
  back_url: new_players_session_path,
  title: "Crear una nueva contraseña" do %>
  <div class="py-6">
    <%= form_with model: @player, url: players_passwords_path do |f| %>
      <div class="flex flex-col gap-2">
        <p>Por favor, ingresá tu email. Si está registrado, te enviaremos un link para que puedas crear una nueva contraseña.</p>
        <%= f.email_field :email, placeholder: "Email" %>

        <%= f.submit "Enviar instrucciones", class: "mt-2 btn btn-primary" %>
      </div>
    <% end %>
  </div>
<% end %>
